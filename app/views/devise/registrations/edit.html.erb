<%= react_component('Profile', {
  form_authenticity_token: form_authenticity_token,
  user: current_user,
  has_slack_id: !current_user.employee.slack_id.blank?,
  company_slack: !current_user.employee.company.blank?,
  settings: current_user.employee.employee_setting
}) %>
