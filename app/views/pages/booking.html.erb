<%= react_component('Booking', {
  defaultOffice: @current_office,
  form_authenticity_token: form_authenticity_token,
  currentDateTime: Time.zone.now,
  tomorrowDateTime: Time.zone.now + 1.day
}) %>
